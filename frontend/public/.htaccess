# HTML immer revalidieren, damit Updates (z. B. Skript-Fixes) sofort ankommen.
# Gehashte Assets (_astro) dürfen lange gecacht bleiben.
<IfModule mod_headers.c>
  <FilesMatch "\.html$">
    Header set Cache-Control "no-cache, must-revalidate"
  </FilesMatch>
</IfModule>
